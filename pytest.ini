[pytest]
addopts = -v --tb=short
markers =
    smoke: fast sanity tests
    regression: full regression suite
    api: API-only tests
    ui: UI-only tests
filterwarnings = ignore::DeprecationWarning
